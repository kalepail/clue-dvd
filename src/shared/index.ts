/**
 * Shared Module Index
 *
 * Re-exports all shared types and constants for easy importing.
 */

// Game elements (suspects, items, locations, times, themes)
export * from "./game-elements";

// API types (requests, responses, common types)
export * from "./api-types";
